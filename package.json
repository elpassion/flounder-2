{
  "name": "@elpassion/flounder-repo",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm",
  "scripts": {
    "build": "turbo run build",
    "create-changeset": "changeset add",
    "publish-packages": "pnpm build && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "prettier": "^2.8.4",
    "turbo": "^1.7.4"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
